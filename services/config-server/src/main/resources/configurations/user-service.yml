server:
  port: 8091
logging:
  level:
    root: INFO
    org:
      springframework:
        web: INFO
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/sbuser
    username: oussema
    password: oussema
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    baseline-description: "init"
    locations: classpath:db/migration
application:
  security:
    jwt:
      secret-key: 8qawA7iafAvGyLtKrm85wCfKs37neK8fflMq7TcvRj8= # generate with: openssl rand -base64 32
      expiration: 86400000 # 24 hours in ms
